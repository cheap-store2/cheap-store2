<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Cheap Store - Electrónica al mejor precio</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
  <style>
    :root {
      --bg-color: #121212;
      --text-color: #ffffff;
      --accent-color: #00ff00; /* Verde dólar */
      --card-bg: #1e1e1e;
      --highlight-color: #ffcc00;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Inter', sans-serif;
    }

    body {
      background-color: var(--bg-color);
      color: var(--text-color);
      padding: 20px;
    }

    header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 20px;
      background-color: var(--bg-color);
      border-bottom: 2px solid var(--accent-color);
    }

    header h1 {
      font-size: 2.5rem;
      color: var(--accent-color);
      text-transform: uppercase;
      letter-spacing: 2px;
    }

    nav {
      display: flex;
      gap: 20px;
    }

    nav a {
      color: var(--text-color);
      text-decoration: none;
      font-weight: bold;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    nav a:hover {
      color: var(--highlight-color);
    }

    .search-bar {
      max-width: 500px;
      margin: 30px auto;
      display: flex;
    }

    .search-bar input {
      flex: 1;
      padding: 12px 20px;
      border: none;
      border-radius: 5px 0 0 5px;
      font-size: 1rem;
      background-color: #2a2a2a;
      color: var(--text-color);
    }

    .search-bar button {
      padding: 12px 20px;
      border: none;
      background-color: var(--accent-color);
      color: #000;
      font-weight: bold;
      border-radius: 0 5px 5px 0;
      cursor: pointer;
    }

    .products {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 20px;
      margin-top: 20px;
    }

    .product-card {
      background-color: var(--card-bg);
      border-radius: 10px;
      padding: 20px;
      transition: transform 0.2s ease;
      cursor: pointer;
    }

    .product-card:hover {
      transform: scale(1.05);
    }

    .product-card img {
      width: 100%;
      height: auto;
      border-radius: 8px;
    }

    .product-card h3 {
      margin: 15px 0 10px;
      font-size: 1.2rem;
      color: var(--accent-color);
    }

    .product-card p {
      font-size: 0.9rem;
      color: #ccc;
    }

    .product-card .price {
      font-size: 1.1rem;
      font-weight: bold;
      color: var(--highlight-color);
      margin-top: 10px;
    }

    .product-card .rating {
      margin-top: 10px;
      color: #ffcc00;
    }

    .product-detail {
      display: none;
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-color: rgba(0, 0, 0, 0.8);
      justify-content: center;
      align-items: center;
      z-index: 1000;
      padding: 20px;
    }

    .product-detail.active {
      display: flex;
    }

    .product-detail .content {
      background-color: var(--bg-color);
      padding: 20px;
      border-radius: 10px;
      max-width: 800px;
      width: 100%;
      display: flex;
      gap: 20px;
    }

    .product-detail .content img {
      width: 50%;
      border-radius: 8px;
    }

    .product-detail .content .info {
      width: 50%;
    }

    .product-detail .content .info h2 {
      color: var(--accent-color);
      margin-bottom: 10px;
    }

    .product-detail .content .info p {
      margin-bottom: 20px;
      color: #ccc;
    }

    .product-detail .content .info .price {
      font-size: 1.2rem;
      font-weight: bold;
      color: var(--highlight-color);
      margin-bottom: 20px;
    }

    .product-detail .content .info .rating {
      margin-bottom: 20px;
    }

    .product-detail .content .info .reviews {
      margin-bottom: 20px;
    }

    .product-detail .content .info .add-to-cart {
      background-color: var(--accent-color);
      color: #000;
      padding: 10px 20px;
      border: none;
      font-weight: bold;
      cursor: pointer;
      border-radius: 5px;
    }

    .product-detail .content .info .add-to-cart:hover {
      background-color: #00cc99;
    }

    .product-detail .close {
      position: absolute;
      top: 20px;
      right: 20px;
      font-size: 2rem;
      color: var(--text-color);
      cursor: pointer;
    }
  </style>
</head>
<body>

  <header>
    <h1>Cheap Store</h1>
    <nav>
      <a href="#inicio">Inicio</a>
      <a href="#conocenos">Conócenos</a>
      <a href="#productos">Productos</a>
      <a href="#contactanos">Contáctanos</a>
    </nav>
  </header>

  <div class="search-bar">
    <input type="text" id="searchInput" placeholder="Buscar productos..." onkeyup="filterProducts()">
    <button onclick="filterProducts()">Buscar</button>
  </div>

  <section class="products" id="productList">
    <!-- Producto
::contentReference[oaicite:0]{index=0}
 
